# Flask Boilerplate

A minimal, production-ready Flask app boilerplate.

## Setup

```bash
python -m venv .venv
.venv\Scripts\Activate.ps1 
pip install -r requirements.txt
cp .env.example .env
python run.py
